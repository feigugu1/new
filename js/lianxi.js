// 读取本地数据，封装函数，后面继续使用
//  先封装好，加上判断语句 查看也没有内容，有的话返回这个字，没有返回空数组
//

//键盘事件。获取文本框内内容
//用一个变量获取调用最新的数组内数据
//将获取到的数据以数组>对象的形式push（）  最后在调用函数，存储数据
//
//存储数据，，在将数据从对象的模式转换为字符川，？？？？字符串到对象。对象在到字符串？？

//渲染页面
//先获取最新的数据，循环所有对象，将数据创建li-添加到ol内，在for循环中添加自定义属性，获取下标

//删除操作
//也是根据下标索引删除的